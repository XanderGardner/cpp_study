#include <iostream>
#include <sstream>
#include <vector>
#include <algorithm>
#include <forward_list>
#include <stack>
#include <utility>
#include <iterator>
#include <list>
#include <queue>
#include <set>
#include <unordered_set>
#include <map>
#include <unordered_map>

using namespace std; // not good in practice

// run `chmod 777 test` in terminal and run `./test` for compile and run (quick tests)
int main() {
  ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0); // fast cin/cout
  cout << '\n'; // use '\n' instead of endl for speed (removes flushing)

  /* ~~~~~~~~~~~~~~~~~~~ trying out the language ~~~~~~~~~~~~~~~~~~~ */

  

  /* ~~~~~~~~~~~~~~~~~~~ ~~~~~~~~~~~~~~~~~~~~~~~ ~~~~~~~~~~~~~~~~~~~ */
  cout << '\n' << '\n'; 
}
